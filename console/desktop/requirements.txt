# Build-time dependencies for the desktop console. End users need only the exe
# plus the Microsoft Edge WebView2 runtime (preinstalled on Windows 10/11).
pywebview>=5.0
pyinstaller>=6.0
# pywebview's EdgeChromium (WebView2) backend on Windows needs pythonnet.
pythonnet>=3.0 ; sys_platform == "win32"
